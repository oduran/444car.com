import React from "react";
import { CFooter, CImg } from "@coreui/react";
import Logo from '../components/Content/Images/logo.svg';

const TheFooter = () => {
    return (
        <p></p>
    );
};

export default React.memo(TheFooter);

